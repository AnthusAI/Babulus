# Minimal Python deps for developing Babulus inside this repo.
#
# Babulus is currently used via `python3 -m babulus ...` and relies on:
PyYAML>=6.0
requests>=2.0
boto3>=1.0
# Optional (Python >=3.11): dotyaml>=0.1.3
#
# Planned for later factoring-out:
# - dotconfig (project/user config discovery utilities)
