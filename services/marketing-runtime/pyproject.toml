[project]
name = "marketing-runtime"
version = "0.1.0"
description = "Marketing automation runtime for Babulus."
requires-python = ">=3.11"
dependencies = [
  "boto3>=1.34.0",
  "requests>=2.31.0",
]

[build-system]
requires = ["setuptools>=69.0.0"]
build-backend = "setuptools.build_meta"
